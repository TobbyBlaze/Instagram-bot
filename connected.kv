#:include login.kv

<Connected>:
    BoxLayout:
        orientation: 'vertical'

        Button:
            text: "start browser"
            font_size: 32
            on_press: root.comment(email.text, password.text, comment.text)
        Button:
            text: "Quit"
            font_size: 24
            on_press: root.disconnect()